class AppConstants {
  const AppConstants._();

  static const appName = 'MovieApp';
  static const backendBaseUrl = 'http://10.0.2.2:4000/api';

  static const connectTimeout = Duration(seconds: 8);
  static const receiveTimeout = Duration(seconds: 10);

  static const tvRailHeight = 245.0;
  static const tvCardWidth = 150.0;
  static const tvCardHeight = 225.0;
  static const tvPagePadding = 40.0;
  static const mobilePagePadding = 16.0;
}
